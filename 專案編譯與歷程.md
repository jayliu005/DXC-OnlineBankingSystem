# 專案編譯與歷程

## 2026-07-23 15:42:04 CST

- 執行者：Codex
- 動作：開始 Migration，盤點新舊專案狀態與本機工具版本。
- 涉及檔案或指令：唯讀檢查舊專案 `專案Migration.md`、schema、Entity、JPA 設定及兩個 Git 工作樹；檢查 Java、Maven、Node.js、npm。
- 結果：確認舊專案只作讀取基準且已有未提交修改；新專案原先只有 Git 與 README。可用 Java 21、Maven 3.9.12、Node.js 25.2.1、npm 11.6.2。

## 2026-07-23 15:42:04 CST

- 執行者：Codex
- 動作：建立 Migration 第一階段專案骨架。
- 涉及檔案或指令：Spring Initializr、create-vue、`backend/`、`frontend/`、`database/`、`infra/`、`docs/`。
- 結果：建立 Java 21 / Spring Boot 4.1.0 Maven 後端，以及 Vue / TypeScript / Vue Router / Pinia / ESLint / Prettier 前端骨架；npm 與 Maven 使用隔離的暫存 cache，未修改使用者全域 cache 權限。Spring Initializr 回傳的父版本帶有 Central 不存在的 `.RELEASE` 後綴，已依 Maven Central metadata 校正為 `4.1.0`。

## 2026-07-23 15:48:09 CST

- 執行者：Codex
- 動作：完成 Migration 第一階段 schema、基礎設施與建置驗證。
- 涉及檔案或指令：`V001__create_bank_user.sql`、`V002__create_account.sql`、`V003__create_transaction_rec.sql`、`application.yaml`、`infra/compose.yaml`、後端 Dockerfile、Vue Migration 入口頁、`mvn clean verify`、`npm run format`、`npm run lint`、`npm run build`、`docker compose config --quiet`。
- 結果：後端 Java 21 build 成功，schema 契約測試 1 筆通過；前端格式化、lint、型別檢查與 production build 全數通過；Compose 設定有效。三個 migration 僅建立空白 schema，不含刪表及資料異動語句。

## 2026-07-23 15:50:25 CST

- 執行者：Codex
- 動作：以獨立 Oracle 23 Free 空白 schema 執行實際 Flyway 與啟動驗證。
- 涉及檔案或指令：`infra/compose.yaml`、驗證用 host port `11521`、驗證用 volume `dxc_onlinebanking_oracle_validation`、Spring Boot Jar。
- 結果：Oracle health check 通過；Flyway 成功套用 V001 至 V003；Spring Boot 以 Java 21 正常啟動；schema 只有 `BANK_USER`、`ACCOUNT`、`TRANSACTION_REC` 與 Flyway history table，三張業務表資料筆數均為 0。驗證完成後已正常停止應用程式，並移除驗證專用容器、network 與 volume，未影響其他占用 `1521` 的 Oracle 服務。

## 2026-07-23 16:08:17 CST

- 執行者：Codex
- 動作：將本機預設 Node.js 從 25 改為 Homebrew Node 24 LTS。
- 涉及檔案或指令：`brew uninstall node`、`brew install node@24`、`brew link --overwrite --force node@24`、`npm run build`。
- 結果：Node 25.2.1 已移除；Node 24.18.0 與 npm 11.16.0 已安裝並由 `/opt/homebrew/bin` 作為新 shell 預設。舊 npm 殘留檔案已由 Homebrew Node 24 版本取代；Vue production build 驗證成功。

## 2026-07-23 16:14:41 CST

- 執行者：Codex
- 動作：統一新專案內部識別名稱為 `dxconlinebanking`。
- 涉及檔案或指令：Maven `artifactId/name`、Spring application name、Java package 與啟動類、Docker Jar pattern、Vue package/title/route/display、Compose project/schema/volume、Migration 與操作文件；`mvn clean verify`、前端 format/lint/build、空白 Oracle schema 啟動驗證。
- 結果：後端座標為 `com.dxc:dxconlinebanking`，Java package 為 `com.dxc.dxconlinebanking`，啟動類為 `DxcOnlineBankingApplication`，Jar 為 `dxconlinebanking-0.0.1-SNAPSHOT.jar`；Vue package 與顯示名稱為 `dxconlinebanking`；Compose project 為 `dxconlinebanking`，Oracle schema 為 `DXCONLINEBANKING`，volume 預設為 `dxconlinebanking_oracle_data`。後端測試、前端檢查與實際 Flyway 啟動均通過；驗證容器、network 與 volume 已清除。

## 2026-07-23 16:16:17 CST

- 執行者：Codex
- 動作：編譯 `dxconlinebanking` 前後端專案。
- 涉及檔案或指令：後端 Java 21 `mvn clean verify`；前端 Node 24 `npm run build`。
- 結果：後端 BUILD SUCCESS，schema 契約測試 1 筆通過，產生約 63 MB 的 `backend/target/dxconlinebanking-0.0.1-SNAPSHOT.jar`；前端 TypeScript 型別檢查與 Vite production build 成功，產物位於 `frontend/dist/`。

## 2026-07-23 16:20:20 CST

- 執行者：Codex
- 動作：提交前掃描並整理 Git 追蹤範圍。
- 涉及檔案或指令：根目錄、後端與前端 `.gitignore`；`git status --ignored`、未追蹤檔案、大檔與常見機密格式掃描、`git diff --check`、`git fetch origin`。
- 結果：確認提交 43 個新專案原始碼、設定、lockfile、Migration 與操作文件；IDE metadata、Maven/Vite 產物、依賴目錄、cache、個人設定及本機 `.env` 均已排除。未發現私鑰、Token、真實密碼或 1 MB 以上非產物檔案；`.env.example` 僅含明確替換用 placeholder。

## 2026-07-23 16:37:24 CST

- 執行者：Codex
- 動作：新增 Git commit message 語言規範。
- 涉及檔案或指令：`AGENTS.md`、`專案編譯與歷程.md`。
- 結果：後續 Git commit message 必須使用繁體中文撰寫，並清楚描述該次提交的變更內容。

## 2026-07-23 16:55:38 CST

- 執行者：Codex
- 動作：完成註冊、登入與登出 Migration 功能切片及獨立環境驗證。
- 涉及檔案或指令：唯讀分析舊系統註冊畫面、Flow、User Service／DAO／Entity、Spring Security 與 Oracle schema；新增 `auth/`、`user/`、`config/SecurityConfig.java`、`common/`、`V004__create_hibernate_sequence.sql`、Vue 登入／註冊畫面、Pinia store、Router guard、API client、`docs/註冊與登入Migration.md`；執行 Java 21 `mvn clean verify`、Node 24 format／lint／build、隔離 Oracle 23 Free 與 REST API 驗證。
- 結果：確認舊 schema 實際使用 `HIBERNATE_SEQUENCE`；全新 schema 成功套用 V001–V004且 JPA validation 通過。註冊後自動登入、session 查詢、使用者名稱唯一性、BCrypt 密碼、登出、錯誤密碼拒絕及重新登入皆符合預期；帳戶與交易表維持空白。驗證用應用程式、容器、network、volume 與暫存 HTTP 檔案已清除。

## 2026-07-23 17:27:30 CST

- 執行者：Codex
- 動作：啟動完整 `dxconlinebanking` 本機系統。
- 涉及檔案或指令：`infra/compose.yaml` Oracle service、Java 21 Spring Boot Jar、Node 24 Vite dev server、`frontend/vite.config.ts`、`README.md`；健康檢查 `/actuator/health`、Vue 首頁與 `/api/auth/session` proxy。
- 結果：保留已占用 `1521` 與 `8080` 的舊系統服務，新系統改用 Oracle `11521`、Spring Boot `18080`、Vue `5173`；Oracle health 為 healthy，後端 health 為 UP，Vue 首頁回傳 200，未登入 session 經 Vue proxy 正確回傳 401。三個新系統服務保持執行中。

## 2026-07-23 18:01:21 CST

- 執行者：Codex
- 動作：將註冊、登入與帳戶首頁 Vue 畫面重製為舊系統版型，並新增前端 Migration 規範。
- 涉及檔案或指令：唯讀比對舊專案 `general.xhtml`、`welcome.xhtml`、`signUp.xhtml`、`accountHome.xhtml` 與 `screen.css`；複製必要圖片及 favicon 至 `frontend/src/assets/legacy/` 與 `frontend/public/`；新增 `LegacyBankingLayout.vue`，調整三個 Vue view、共用 CSS、頁面標題及 `AGENTS.md`；執行 Node 24 format、lint、type-check、production build，並以本機 Chrome headless 截圖比對新舊登入頁與檢查註冊頁。
- 結果：Vue 頁首、綠色標題列、紋理背景、fieldset、欄位順序、文案、按鈕與帳戶選單配置已貼近舊版；註冊、登入與登出 API 行為保持不變，尚未 Migration 的帳戶功能以停用按鈕呈現。複製資源為舊檔案的未修改副本，舊專案未被變更；前端格式、lint、型別檢查及 production build 全數通過。

## 2026-07-23 18:05:23 CST

- 執行者：Codex
- 動作：更新 Vue 前端品牌名稱。
- 涉及檔案或指令：`LegacyBankingLayout.vue`、`frontend/src/router/index.ts`。
- 結果：頁首、頁尾及瀏覽器標題的 `Green's Online Banking System` 已統一改為 `DXC Online Banking System`。

## 2026-07-23 18:34:21 CST

- 執行者：Codex
- 動作：完成註冊登入與舊版前端畫面功能切片的提交前檢查及 GitHub 推送。
- 涉及檔案或指令：待提交檔案、忽略檔案、大型檔案與機密格式掃描；Java 21 `mvn clean verify`；Node 24 `npm run lint`、`npm run build`；Git commit 與 push。
- 結果：未發現不應提交的建置產物或真實機密；後端 BUILD SUCCESS，前端 lint、型別檢查及 production build 通過；本次功能以繁體中文 commit message 提交並推送至 GitHub `main` 分支。

## 2026-07-24 15:30:11 CST

- 執行者：Codex
- 動作：完成帳戶清單與建立帳戶 Migration 功能切片。
- 涉及檔案或指令：唯讀分析舊系統 `account-flow.xml`、帳戶 XHTML、`BankAccount`、Account Service／DAO 與 schema；新增後端 `account/` Entity、Repository、Service、Controller、request／response 與單元測試；新增 Vue account API、type、建立帳戶畫面及舊版成功資源；更新帳戶首頁、REST client、安全設定、README 與 `docs/帳戶Migration.md`；執行 Java 21 `mvn clean verify`、Node 24 format／lint／build及 Oracle REST 整合驗證。
- 結果：目前登入使用者可列出自己的帳戶，建立 Checking／Saving 帳戶時會驗證 4 位數 PIN、以 BCrypt 儲存 PIN、設定 `0.00` 初始餘額及伺服器建立時間；未登入 API 回傳 `401`。後端 2 筆測試、前端格式／lint／型別檢查／production build、Flyway、Hibernate validation、Session 與 Vite proxy 均通過；驗證用使用者、帳戶、Cookie 與暫存檔已清除。新 Oracle、後端與 Vue 服務保持執行。

## 2026-07-24 15:50:20 CST

- 執行者：Codex
- 動作：完成存款與提款 Migration 功能切片。
- 涉及檔案或指令：唯讀分析舊系統 `deposit.xhtml`、`withdraw.xhtml`、`tranSucceed.xhtml`、`TransactionRec`、Transaction Service／DAO、Account DAO 與訊息規則；新增後端 `transaction/` Entity、Repository、Service、Controller、request／response、例外與單元測試；新增 Vue transaction API、type、存提款與成功畫面，啟用首頁存提款選單；更新 README 與 `docs/存款與提款Migration.md`；執行 Java 21 `mvn clean verify`、Node 24 format／lint／build及 Oracle REST 整合驗證。
- 結果：存款與提款會驗證目前使用者帳戶、舊版金額範圍、BCrypt PIN 與可用餘額，並在同一資料庫交易中更新餘額及建立 `Deposit`／`Withdraw` 紀錄。後端 6 筆測試與前端全部檢查通過；實際成功存款 `100.50`、提款 `40.25` 後餘額為 `60.25`，錯誤 PIN、餘額不足、超過上限及他人帳戶均被拒絕且未異動資料。驗證資料與暫存敏感資訊已清除，系統保持執行。

## 2026-07-24 15:52:55 CST

- 執行者：Codex
- 動作：提交帳戶、存款與提款 Migration 程式碼。
- 涉及檔案或指令：提交前 Git 追蹤範圍、忽略檔案、大型檔案與機密格式掃描；Git add 與 commit。
- 結果：確認測試資料、Cookie、暫存 response、建置產物、依賴目錄、本機設定及真實機密均未納入；帳戶清單／建立帳戶、存款／提款、測試與 Migration 文件以繁體中文 commit message 建立本機提交，未執行 push。

## 2026-07-24 15:56:24 CST

- 執行者：Codex
- 動作：將帳戶、存款與提款 Migration 程式碼推送至 GitHub。
- 涉及檔案或指令：`git fetch origin`、Git 歷程紀錄 commit、`git push origin main`。
- 結果：確認本機 `main` 與遠端沒有分歧；帳戶、存款與提款功能提交及本紀錄已推送至 GitHub `origin/main`。

## 2026-07-24 16:19:40 CST

- 執行者：Codex
- 動作：完成轉帳 Migration 功能切片。
- 涉及檔案或指令：唯讀分析舊系統 `transfer.xhtml`、`tranSucceed.xhtml`、Transaction Service 與訊息規則；新增目的帳戶存在性 API、轉帳 request／Service／Controller 與單元測試；新增 Vue 轉帳 API、type、舊版轉帳與成功畫面，啟用首頁轉帳選單；更新 README 與 `docs/轉帳Migration.md`；執行 Java 21 `mvn clean verify`、Node 24 format／lint／build及 Oracle REST 整合驗證。
- 結果：來源帳戶會驗證登入使用者歸屬、BCrypt PIN、金額上限與可用餘額，目的帳戶可跨使用者但必須存在；兩個帳戶餘額與 `Transfer` 紀錄在同一資料庫交易中更新。後端 10 筆測試及前端全部檢查通過；實際由不同使用者帳戶轉帳 `75.50` 後來源與目的餘額分別為 `124.50`、`75.50`，錯誤 PIN、同帳戶、帳戶不存在、餘額不足、超過上限、他人來源帳戶與未登入均正確拒絕且未異動資料。驗證資料與 Cookie 已清除，原有資料未變更；Oracle、後端與 Vue 服務保持執行。

## 2026-07-24 16:58:23 CST

- 執行者：Codex
- 動作：完成交易紀錄 Migration 功能切片。
- 涉及檔案或指令：唯讀分析舊系統 `tranHistory.xhtml`、`tranHistory2.xhtml`、`account-flow.xml`、Transaction Service／DAO／Entity；新增交易紀錄查詢 Repository、Service、Controller、response 與 Vue API、type、查詢及結果畫面；啟用首頁交易紀錄選單；更新 README 與 `docs/交易紀錄Migration.md`；執行 Java 21 `mvn test`、`mvn package -DskipTests`、Node 24 format／lint／build及 Oracle REST 整合驗證。
- 結果：目前登入使用者可依本人帳戶與起訖日期查詢 Deposit、Withdraw、Transfer 紀錄；轉帳會依來源／目的顯示 Send／Receive Note，日期範圍包含結束日。後端 10 筆測試及前端全部檢查通過；實際來源與目的帳戶查詢均正確，Vite proxy 回傳 `200`，他人帳戶回傳 `404`，無效日期回傳 `400`。驗證資料與 Cookie 已清除，原有資料未變更；Oracle、後端與 Vue 服務保持執行。

## 2026-07-24 18:23:03 CST

- 執行者：Codex
- 動作：完成個人資料查詢／修改 Migration 功能切片。
- 涉及檔案或指令：唯讀分析舊系統 `userInfo.xhtml`、`userInfoUpdated.xhtml`、`account-flow.xml`、User Service／DAO／Entity 與 Bean Validation；新增 `user/` profile request／response／Service／Controller 與可編輯欄位更新方法；新增 Vue profile API、type、舊版個人資料編輯及成功摘要畫面，啟用帳戶首頁 User Profile；更新 README 與 `docs/個人資料Migration.md`；執行 Java 21 `mvn test`、`mvn package -DskipTests`、Node 24 format／lint／build及 Oracle REST 整合驗證。
- 結果：登入使用者可讀取及修改本人資料，user name、密碼與 ID 不可修改；欄位長度、Gender、出生日期、Zip、Phone、Email 驗證符合舊系統。後端 10 筆測試及前端全部檢查通過；實際 GET／PUT profile 成功，空白 Middle Initial 儲存為 `NULL`，非法資料回傳 `400`，未登入回傳 `401`，Vite proxy 回傳 `200`。驗證資料與 Cookie 已清除，原有資料未變更；Oracle、後端與 Vue 服務保持執行。
